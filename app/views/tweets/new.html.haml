.contents.row1234
  .container4
    = form_with(model: @tweet, local: true) do |form|
      %h3
        血圧記録
      = form.text_field :name, placeholder: "日付"
      = form.text_area :text, placeholder: "血圧、体調等" , rows: "10"
      = form.submit "登録"

